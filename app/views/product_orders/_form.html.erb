<%= form_with(model: product_order) do |form| %>
  <% if product_order.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_order.errors.count, "error") %> prohibited this product_order from being saved:</h2>

      <ul>
        <% product_order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.number_field :product_id %>
  </div>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.number_field :order_id %>
  </div>

  <div>
    <%= form.label :product_quatity, style: "display: block" %>
    <%= form.number_field :product_quatity %>
  </div>

  <div>
    <%= form.label :product_unit_cost, style: "display: block" %>
    <%= form.number_field :product_unit_cost %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
